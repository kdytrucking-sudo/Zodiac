# Webhook Configuration
# Copy this file to .env and fill in your values

# Webhook Secret Token (change this to a secure random string)
WEBHOOK_SECRET_TOKEN=wh_your_secret_token_here_change_this_12345

# Base URL for generating article links
BASE_URL=http://localhost:8080

# Firebase Admin SDK (if not already configured)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/serviceAccountKey.json

# Google AI API Key (already configured)
# GOOGLE_GENAI_API_KEY=your_key_here
